package com.example.matchmakers.match

import com.google.firebase.firestore.auth.User

class MatchAlgorithm {

    fun calculateCompatibilityScore(user1: User, user2: User): Double {
        // example logic for calculating compatibility score
        var score = 0.0
        // add matching logic here
        return score
    }
}
